# Install cheat sheet

This file will be filled in by issue #2 (`002-install-cheat-sheet.md`).

For now: create `~/.config/ser8-setup/secrets.env` with your secrets (any shell-sourceable file, even empty) before running `./run.sh`.
